/**
 * SortSelector — sort order control.
 *
 * Props:
 *   options  {Array<{value, label}>}
 *   value    {string}                 current sort value
 *   onChange {function}               (value) => void
 *   variant  {"dropdown"|"tabs"}
 */
export function SortSelector({ options = [], value, onChange, variant = 'dropdown' }) {
  return null;
}
