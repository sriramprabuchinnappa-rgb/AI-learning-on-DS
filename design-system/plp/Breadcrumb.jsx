/**
 * Breadcrumb — hierarchical navigation trail.
 *
 * Props:
 *   items     {Array<{label, href?}>}
 *   separator {string|ReactNode}       default "/"
 *   maxItems  {number}                 collapses middle items beyond this count
 *   collapsed {boolean}
 */
export function Breadcrumb({ items = [], separator = '/', maxItems, collapsed = false }) {
  return null;
}
